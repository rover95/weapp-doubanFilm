<view wx:for="{{arr}}" wx:for-item="i" wx:key="*this">{{index}}---{{i}} </view>
<view wx:if="{{arr[1] == 'qws'}}" >if</view>
<map name="asd"></map>

<view>
  <include src="../page2/page2"></include>
</view>